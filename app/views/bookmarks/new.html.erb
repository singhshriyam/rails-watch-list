<div class="container mt-5">
  <h1>Add a new bookmark for <%= @list.name %></h1>

  <%= simple_form_for([@list, @bookmark]) do |f| %>
  <%= f.input :comment %>
  <%= f.association :movie %>
  <%= f.button :submit %>
<% end %>
</div>
